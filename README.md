# deekshakalia.github.io

My personal website and blog at https://deekshakalia.github.io/

## Local Development

Install dependencies:

```bash
bundle install
```

Run the Jekyll development server:

```bash
bundle exec jekyll serve
```

The site will be available at `http://localhost:4000`. Add `--livereload` to auto-refresh on file changes.
